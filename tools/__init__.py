"""Routing-tuning tools for cobaiter (dev-only; not part of the built package).

Run each as a module from the repo root, e.g. ``python -m tools.sim_router``.
See tools/README.md for the workflow.
"""
